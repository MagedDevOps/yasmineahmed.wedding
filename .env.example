# Vercel env vars are no longer required for RSVP.
# Guests use a Google Form; you view replies in your Google Sheet.
#
# Dashboard password is set in dashboard.js (DASHBOARD_PASSWORD).
# Google Form URL is set in script.js (CONFIG.googleFormUrl).
